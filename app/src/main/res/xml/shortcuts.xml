<shortcuts xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <shortcut
        android:enabled="true"
        android:icon="@drawable/shortcut_metronome"
        android:shortcutId="metronome"
        android:shortcutShortLabel="@string/shortcuts_metronome"
        tools:ignore="UnusedAttribute">
        <intent
            android:action="android.intent.action.VIEW"
            android:targetClass="com.vsevolodganin.clicktrack.MainActivity"
            android:targetPackage="${applicationId}">
            <extra
                android:name="destination"
                android:value="metronome" />
        </intent>
    </shortcut>
</shortcuts>
